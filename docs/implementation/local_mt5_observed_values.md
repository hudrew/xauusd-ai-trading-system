# 本地 MT5 观察值记录

## 2026-03-29 会话观察

### 账户与终端

- 日期：`2026-03-29`
- 账户类型：待进一步人工确认
  说明：服务器名为 `TradeMaxGlobal-Live`，但当前只确认了登录链路，不应仅凭服务器名推断可交易状态
- 账户号：`50182922`
- 服务器名：`TradeMaxGlobal-Live`
- 终端版本：`MetaTrader 5 x64 build 5715`
- 终端路径：`/Applications/MetaTrader 5.app`
- 登录结果：已成功登录并落盘
  说明：终端主日志记录了 `2026-03-29 03:32:23` 与 `2026-03-29 03:32:48` 两次 `authorized on TradeMaxGlobal-Live`

### 连接与同步

- 服务器目录已生成：`Bases/TradeMaxGlobal-Live`
- 终端同步结果：`0 positions, 0 orders, 437 symbols, 0 spreads`
- 访问节点：`Access Server-SZ1`
- 当前 Ping：约 `250.05 ms`

### 当前阻塞

- 服务器端交易状态：`trading has been disabled - disabled on server`
- 结论：当前已经不是网络或登录问题，而是账户在券商服务器侧没有交易权限

### 品种

- 券商实际黄金品种名：`XAUUSD`
- 是否与 `XAUUSD` 完全一致：是
- 价格小数位数：`2`
- 最小变动单位：待进一步确认
  说明：截图里已看到 `小数位=2`，但未直接看到最小报价跳动值字段
- 合约量：`100`
- 点差模式：`浮动`
- 品种交易模式：`完全访问`
  说明：这表示品种层面支持交易，不等同于当前账户一定有交易权限
- 执行模式：`市价`
- 订单成交模式：`全部或部分 (IOC)`

### 手数限制

- 最小手数：`0.01`
- 手数步长：`0.01`
- 最大手数：`80`

### 止损止盈限制

- 最小止损距离：`0`
- 最小止盈距离：`0`
- 是否存在特殊限制：待确认
  说明：截图中的 `停损级别=0`，通常表示品种未设置额外的最小止损距离限制，但真实下单仍要以服务器返回结果为准

### 点差观察

- 正常时段点差：约 `0.11`
  说明：基于 `2026-03-29` 提供的市场深度截图，首档卖价约 `4495.67`，首档买价约 `4495.56`，即时点差约 `0.11`
- 欧盘时段点差：待确认
- 美盘时段点差：待确认
- 新闻前后点差：待确认
- 是否存在异常扩张：待确认
  说明：规格页只显示 `点差=浮动`，不提供实时数值，需要在 `Market Watch` 或下单面板里持续观察

### 手工小单结果

- 最小单是否成交：已验证
  说明：在另一组允许交易的测试账号上，`XAUUSD` 最小手数手工单已成功成交
- 平仓是否正常：已验证
  说明：测试账号手工最小单已成功平仓
- 止损是否正常：待确认
- 止盈是否正常：待确认
- 滑点情况：待进一步量化记录
- 手续费情况：待进一步量化记录
- 拒单或错误说明：
  - `TradeMaxGlobal-Live` 当前这组 `50182922` 账户：服务器日志返回 `trading has been disabled - disabled on server`
  - 独立测试账号：最小手工单开平成功，说明品种、最小手数、执行模式本身没有硬性阻塞

### 其他已观察信息

- 库存费类型：`点模式`
- 买入库存费：`-64.58`
- 卖出库存费：`35.92`
- 手续费：`5 USD / 手`
- 交易时段：
  - 周一到周四：行情 `01:00-23:59`，交易 `01:01-23:59`
  - 周五：行情 `01:00-23:55`，交易 `01:01-23:55`

## 建议回填到配置

- `XAUUSD_AI_MT5_SERVER=TradeMaxGlobal-Live`
- `XAUUSD_AI_MT5_PATH=/Applications/MetaTrader 5.app`
- `XAUUSD_AI_MT5_SYMBOL=XAUUSD`
- `execution.mt5.deviation=30`
- `risk.contract_size=100`
- `XAUUSD_AI_RISK_CONTRACT_SIZE=100`
- `risk.max_spread_ratio=1.40`
- `volatility_monitor.spread_ratio_trigger=1.20`
  说明：这里的 `spread_ratio` 是当前点差相对近 60 根中位点差的比值，不是绝对点差值；`1.20` 代表点差较近期常态扩张约 20% 时开始触发高波动观察

## 当前判断

- 是否适合继续手工联调：适合
  说明：登录、升级、服务器切换、日志落盘、黄金规格核对、测试账号最小手工单都已确认
- 是否适合进入执行宿主机阶段：适合进入下一阶段准备
  说明：业务侧联调已经具备进入执行宿主机阶段的条件，但当前 Mac 仍不能作为 MT5 Python 执行宿主机
- 当前最大风险点：live 账户和测试账户的服务器权限可能不一致，不能把测试账号成功直接等同于 live 可下单
- 下一步动作：
  1. 保留当前测试账号作为纸面盘 / 执行宿主机首轮联调账户
  2. 让券商或后台确认 `50182922` 这组 live 账户为何被禁用交易
  3. 在独立 Windows 执行宿主机上准备 `.env.mt5.local` 并跑 `host-check` 和 `preflight`
  4. 先跑 `paper loop`，稳定后再决定是否切 live
